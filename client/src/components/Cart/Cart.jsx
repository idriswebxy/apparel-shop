import { Container } from "@mui/material"
import React from "react"
import CartDivider from "./CartDivider"

const Cart = (props) => {
  return (
    <Container>
      <CartDivider />
    </Container>
  )
}

export default Cart
